/**
 * 韓国の地形(海岸線・地形帯・河川・湖沼・地名ラベル・盤面装飾)。
 *
 * 座標は [経度, 緯度]。投影は他国と同じ線形投影。
 * 緯度1度と経度1度の見た目の距離が揃うよう、盤面の幅は
 *
 *   BW = BH × (経度の幅 ÷ 緯度の幅) × cos(平均緯度)
 *
 * で決める(フランスの geography.mjs が書き残した式。**度数の幅を掛け忘れて
 * `BH × cos` だけにすると国の形が縦に伸びる**ので注意)。
 *
 * 経度125.5〜131.0度(鬱陵島まで)、緯度38.7〜33.0度(済州島まで)。
 * 経度幅5.5度・緯度幅5.7度、平均緯度35.85度の cos(≒0.811)を掛けると
 * 横/縦 ≒ 0.782。都市40件・1件あたり約90,000px²(フランス・日本・インドと同じ
 * 密度)に合わせて BW×BH ≒ 3,600,000 とすると、BH≒2150 / BW≒1680。
 */

/** 投影。東経125.5〜131.0度、北緯38.7〜33.0度。 */
export const KOREA_PROJ = {
  BW: 1680,
  BH: 2150,
  LON0: 125.5,
  LON1: 131.0,
  LAT0: 38.7,
  LAT1: 33.0,
  seg: 84,
};

/**
 * 本土の輪郭。北東(高城・DMZが海に出る所)から時計回りに、
 * 東海岸(日本海側) → 南海岸 → 西海岸(黄海側) → 非武装地帯(DMZ)。
 */
const MAINLAND = [
  [128.43, 38.60], // 高城(統一展望台。DMZが東海に出る所)
  [128.64, 38.21], // 束草(町の座標128.59より少し沖に出す)
  [128.75, 37.90], // 襄陽の北
  [128.96, 37.75], // 江陵(町の座標128.90より少し沖に出す)
  [129.03, 37.69], // 正東津の岬
  [129.22, 37.45], // 三陟(町の座標129.16より少し沖に出す)
  [129.30, 37.05],
  [129.40, 36.99], // 蔚珍の岬
  [129.40, 36.55],
  [129.43, 36.02], // 浦項(町の座標129.37より少し沖に出す)
  [129.56, 35.99], // 虎尾串(本土最東端の岬)
  [129.40, 35.70],
  [129.37, 35.54], // 蔚山(町の座標129.31より少し沖に出す)
  [129.20, 35.30], //機張(釜山の北)
  [129.18, 35.10], // 釜山(海雲台)
  [129.00, 35.02], // 釜山(洛東江河口)
  [128.70, 34.85], // 巨済水道の方へ
  [128.42, 34.85], // 統営
  [128.05, 34.73], // 南海
  [127.75, 34.68],
  [127.66, 34.55], // 麗水(南へ突き出す岬)
  [127.52, 34.75], // 麗水半島の西側を北へ
  [127.49, 34.95], // 光陽湾・順天湾
  [127.20, 34.78],
  [126.90, 34.60],
  [126.65, 34.47], // 莞島の方(本土最南部)
  [126.45, 34.55],
  [126.39, 34.79], // 木浦
  [126.35, 35.15],
  [126.47, 35.40],
  [126.55, 35.65], // 辺山の辺り
  [126.72, 35.97], // 群山
  [126.58, 36.15], // 舒川の岬
  [126.50, 36.33], // 保寧
  [126.35, 36.55], // 泰安の岬(西へ突き出す)
  [126.50, 36.80], // 泰安半島の東側・牙山湾
  [126.65, 37.05], // 平沢の海岸
  [126.58, 37.30], // 南陽湾の入り込み
  [126.55, 37.46], // 仁川の南(松島)
  [126.63, 37.58], // 仁川港
  [126.55, 37.75], // 漢江河口へ
  [126.68, 37.80], // 漢江・臨津江の河口
  [126.50, 38.05], // DMZが海に出る辺り(板門店より西を保つ)
  [126.85, 38.18], // DMZ
  [127.25, 38.12], // DMZ(鉄原の張り出し)
  [127.60, 38.30], // DMZ
  [128.00, 38.35], // DMZ
  [128.43, 38.60], // 高城へ戻って閉じる
];

/** 済州島。楕円に近い輪郭。 */
const JEJU = [
  [126.15, 33.38],
  [126.22, 33.50],
  [126.35, 33.56],
  [126.55, 33.57],
  [126.75, 33.53],
  [126.87, 33.44],
  [126.92, 33.34],
  [126.83, 33.23],
  [126.65, 33.16],
  [126.45, 33.15],
  [126.28, 33.20],
  [126.16, 33.30],
  [126.15, 33.38],
];

/** 鬱陵島。ごく小さな島。 */
const ULLEUNGDO = [
  [130.85, 37.49],
  [130.88, 37.52],
  [130.93, 37.51],
  [130.95, 37.47],
  [130.91, 37.44],
  [130.86, 37.45],
  [130.85, 37.49],
];

export const KOREA_LAND = [MAINLAND, JEJU, ULLEUNGDO];

/**
 * 地形帯。陸地でクリップして描かれる。
 * 太白山脈(東海岸沿いの背骨) → 小白山脈(嶺南と湖南を分ける斜めの壁) →
 * 湖南平野(西南部の穀倉地帯) → 京畿の平野 → 漢拏山(済州の火山)。
 */
export const KOREA_TERRAIN = [
  // 太白山脈(東海岸沿い、北から南へ続く背骨)
  ["#5f7f4a", [[128.55, 38.55], [128.85, 38.15], [129.05, 37.60], [129.20, 37.00], [129.15, 36.30], [128.95, 36.30], [128.80, 37.00], [128.65, 37.70], [128.35, 38.35]]],
  // 小白山脈(嶺南と湖南を分ける斜めの山地)
  ["#6f8a52", [[128.55, 37.05], [128.95, 36.35], [128.55, 35.45], [128.00, 35.15], [127.75, 35.55], [128.15, 36.35], [128.30, 36.85]]],
  // 京畿の平野(漢江の下流、ソウルの周り)
  ["#a8bd6a", [[126.55, 37.85], [127.15, 37.75], [127.25, 37.30], [126.85, 37.05], [126.55, 37.35], [126.50, 37.65]]],
  // 湖南平野(全羅道の穀倉地帯)
  ["#b3c26a", [[126.55, 36.10], [127.15, 35.95], [127.10, 35.35], [126.95, 34.95], [126.55, 34.95], [126.40, 35.45], [126.45, 35.85]]],
  // 嶺南の盆地(大邱の周り、りんご畑)
  ["#9aa85c", [[128.30, 36.10], [128.85, 35.95], [128.75, 35.55], [128.30, 35.55], [128.15, 35.85]]],
  // 済州の漢拏山(火山の裾野)
  ["#8a7a5c", [[126.42, 33.47], [126.55, 33.53], [126.68, 33.47], [126.60, 33.35], [126.45, 33.35]]],
];

/** 河川。韓国の主要4河川。 */
export const KOREA_RIVERS = [
  // 漢江(江原の山地 → 春川 → ソウル → 黄海)
  [[127.75, 38.05], [127.73, 37.88], [127.30, 37.75], [126.98, 37.57], [126.68, 37.80]],
  // 洛東江(安東 → 大邱 → 釜山)
  [[128.80, 36.75], [128.73, 36.57], [128.60, 35.87], [128.75, 35.40], [129.00, 35.10]],
  // 錦江(清州の南 → 大田 → 扶余 → 群山)
  [[127.55, 36.55], [127.38, 36.35], [127.10, 36.28], [126.91, 36.28], [126.72, 35.97]],
  // 栄山江(光州 → 木浦)
  [[126.90, 35.25], [126.85, 35.16], [126.60, 34.95], [126.39, 34.79]],
];

/**
 * 湖沼 [経度, 緯度, 半径x, 半径y, 回転, 色]。**半径は盤面ピクセル。度ではない。**
 * ここを度のつもりで書くと1px未満になり画面から消える(インド・フランスで
 * 実際に起きた)。韓国は経度1度≒305px・緯度1度≒377px。
 */
export const KOREA_LAKES = [
  [127.83, 38.07, 20, 12, -20, "#3f8fc4"], // 昭陽湖(春川の北。ダム湖)
  [127.95, 36.95, 12, 22, 15, "#4f9fd0"], // 忠州湖(南北に細長いダム湖)
];

function t(source) {
  const [en, es, fr, ja] = source.split("|");
  return { en, es, fr, ja };
}

/** 地名ラベル [経度, 緯度, 文言, 水域か]。 */
export const KOREA_LABELS = [
  [130.3, 37.0, t("EAST SEA|MAR DEL ESTE|MER DE L'EST|東海"), 1],
  [126.0, 35.6, t("YELLOW SEA|MAR AMARILLO|MER JAUNE|黄海"), 1],
  [127.3, 34.3, t("KOREA STRAIT|ESTRECHO DE COREA|DÉTROIT DE CORÉE|朝鮮海峡"), 1],
  [129.0, 37.3, t("TAEBAEK MOUNTAINS|MONTES TAEBAEK|MONTS TAEBAEK|太白山脈"), 0],
  [128.3, 36.0, t("SOBAEK MOUNTAINS|MONTES SOBAEK|MONTS SOBAEK|小白山脈"), 0],
  [126.53, 33.36, t("MT HALLA|MONTE HALLA|MONT HALLA|漢拏山"), 0],
  [127.2, 38.0, t("DMZ|DMZ|ZDC|非武装地帯"), 0],
];

/** 装飾の位置。 */
const MOUNTAINS = [
  [128.75, 38.30], // 金剛山に近い辺り
  [128.95, 37.55], // 太白山脈
  [129.10, 36.70],
  [128.45, 36.20], // 小白山脈
  [128.10, 35.35],
  [126.53, 33.36], // 漢拏山
];
const HILLS = [
  [127.0, 37.4], // 京畿の丘陵
  [127.6, 36.0], // 忠清の丘陵
  [127.3, 35.5], // 全羅の丘陵
];
/** 松林(東海岸の砂浜の松)。 */
const PINES = [
  [128.62, 38.05],
  [129.10, 37.30],
  [129.35, 36.20],
];
/** 田んぼ(湖南・京畿の平野)。 */
const PADDIES = [
  [126.75, 35.60],
  [126.95, 35.15],
  [126.70, 37.15],
  [127.35, 36.55],
];

/** 盤面装飾のSVG断片。投影関数は組み立て側から渡される。 */
export function renderKoreaDecor(px, py) {
  const parts = [];
  for (const [lo, la] of MOUNTAINS) {
    const x = px(lo);
    const y = py(la);
    parts.push(
      `<path d="M${(x - 27).toFixed(1)},${y.toFixed(1)}L${x.toFixed(1)},${(y - 36).toFixed(1)}L${(x + 27).toFixed(1)},${y.toFixed(1)}z" fill="#8b8f98" opacity=".85"/>`,
      `<path d="M${(x - 10).toFixed(1)},${(y - 14).toFixed(1)}L${x.toFixed(1)},${(y - 36).toFixed(1)}L${(x + 10).toFixed(1)},${(y - 14).toFixed(1)}z" fill="#f2f6f8"/>`,
    );
  }
  for (const [lo, la] of HILLS) {
    const x = px(lo);
    const y = py(la);
    parts.push(
      `<path d="M${(x - 23).toFixed(1)},${y.toFixed(1)}c7,-15 32,-15 44,0z" fill="#5f7f4a" opacity=".8"/>`,
    );
  }
  for (const [lo, la] of PINES) {
    const x = px(lo);
    const y = py(la);
    parts.push(
      `<rect x="${(x - 2).toFixed(1)}" y="${(y - 17).toFixed(1)}" width="4" height="17" fill="#6b5330"/>`,
      `<path d="M${x.toFixed(1)},${(y - 32).toFixed(1)}l-12,19h24z" fill="#2f6b3a"/>`,
    );
  }
  for (const [lo, la] of PADDIES) {
    const x = px(lo);
    const y = py(la);
    parts.push(
      `<g stroke="#7fa8c4" stroke-width="3" opacity=".75"><path d="M${(x - 16).toFixed(1)},${y.toFixed(1)}h32M${(x - 16).toFixed(1)},${(y + 7).toFixed(1)}h32M${(x - 16).toFixed(1)},${(y + 14).toFixed(1)}h32"/></g>`,
    );
  }
  return parts.join("");
}

export const KOREA_COLORS = {
  sea: "#16324f",
  seaWave: "#1e4266",
  landBase: "#8fae63",
  coast: "#2f4a2a",
};
